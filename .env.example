# Example .env for scraper/email notification
# Put a copy named .env in the project root (same folder as scrape.py) and fill in values.

# Gmail account to send from (must allow SMTP via app password)
SMTP_USER=your-email@gmail.com
# App password (create at https://myaccount.google.com/apppasswords) - DO NOT commit this value
SMTP_PASS=your-app-password

# Optional: recipient (defaults to SMTP_USER)
SCRAPE_NOTIFY_TO=recipient@example.com

# Optional: override SMTP server/port (defaults to Gmail)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587

# Notes:
# 1) Install python-dotenv: pip install python-dotenv
# 2) Use an App Password for Gmail if your account has 2FA enabled.
# 3) Never commit .env to version control; add it to .gitignore.
